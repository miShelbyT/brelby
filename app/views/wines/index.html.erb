<h1> Wine Selection: </h1>
<ul>
    <% @wines.each do |wine| %>
        <li> <%= link_to wine.name, wine_path(wine) %> </li>
        <li> <%= link_to image_tag wine.img_url %> </li>
    <% end %>
</ul>